"use client";

const HomepageOSCallout = () => {
  return (
    <section
      id="os-callout"
      className="bg-teal text-paper py-10 md:py-12"
    >
      <div className="max-w-4xl mx-auto px-6 text-center">
        <p className="font-display font-[340] tracking-tight text-[clamp(20px,2.4vw,30px)] leading-snug">
          Stop the leak. Run your clinic on GrowthOS.
        </p>
      </div>
    </section>
  );
};

export default HomepageOSCallout;
